[文件目录]
# 学生源文件根目录，最后的/可以有，也可以没有
# 根目录下每个学生提交目录的编码规则是“课号-学号”，如果一个学生选多门课，则有多个学生提交目录
# 作业检查程序的正式版在Linux下运行，此处要求用“/”来兼容Linux，不要用“\”
src_rootdir = D:/oop/大作业/24251-900106-W1402.综合题 - 第05题 - 学生作业检查/source/

[日志目录]
db_hw_logfile = hw_check.log  # 指定起止文件名
db_hw_logsize = 1048576       # 超过设定值则日志清空

[数据库]
db_host     = 10.80.42.230          # 数据库服务器的IP地址（校园网内）
db_port     = 3306		    # 数据库服务器的端口号
db_name     = homework              # 数据库名称
db_username = hwcheck               # 用户名
db_passwd   = hw_CheCk-For24251*oOP # 用户密码（移除了加解密工具集，此处简化为明码表示想，实际工程中不允许）
db_currterm = 2024/2025/1           # 当前学期

[编译日志]
# 已裁减，暂无

[批处理文件]
# 已裁减，暂无

[成绩计算]
# 已裁减，暂无

[专业全称简称映射表]
# 已裁减，暂无
